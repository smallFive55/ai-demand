# Story 1.2: 管理员管理账号生命周期

Status: done

## Story

As a 管理员,  
I want 创建、编辑、禁用账号并支持批量导入,  
so that 能高效维护组织成员与系统访问。

## Acceptance Criteria

1. **Given** 管理员已进入账号管理页面
  **When** 管理员执行创建、编辑、禁用或批量导入操作  
   **Then** 账号状态与属性按操作结果更新  
   **And** 账号变更关联角色信息并保留操作审计记录

## Tasks / Subtasks

- 建立账号管理领域模型与接口（AC: 1）
  - 在 `apps/api/src/modules/admin` 新增账户聚合（User/Account）与状态字段（启用/禁用）
  - 提供创建、编辑、禁用、批量导入 REST API，并统一返回 `success/error` envelope
  - 对角色关联做输入校验与存在性校验，禁止悬空 roleId
- 实现前端账号管理页关键流程（AC: 1）
  - 在 `apps/web/src/features/admin` 新增账号列表/编辑抽屉/禁用确认弹层/批量导入入口
  - 与 API 打通创建、编辑、禁用、导入流程，操作后刷新列表并展示结果反馈
  - 表单与批量导入错误提示采用“问题 + 原因 + 下一步”结构
- 落地审计与可追溯（AC: 1）
  - 为创建、编辑、禁用、导入写入审计事件（含 actor、target、before/after、时间）
  - 对失败操作保留失败审计（至少记录原因码与请求标识）
- 补充测试与质量门（AC: 1）
  - 后端：账户生命周期接口单测/集成测试
  - 前端：关键交互单测（表单提交、禁用确认、导入失败处理）
  - 回归执行 `pnpm lint`、`pnpm test`、`pnpm build`

## Dev Notes

### Story Foundation

- 本故事对应 FR32，是 Epic 1 治理能力的核心入口；后续角色权限矩阵（Story 1.3）依赖本故事账户与角色关联能力。
- 业务目标是“可维护组织成员访问生命周期”，不是一次性账号创建工具。
- 验收重点：状态变更准确、角色关联正确、审计完整可追溯。

### Technical Requirements

- 后端基线保持 NestJS v11 + Node 22，不得降级到旧运行时。
- 数据存储遵循架构文档（MySQL 8.4 LTS）；命名 `snake_case`、接口字段 `camelCase`。
- API 统一响应结构：成功 `{ success, data, meta }`，失败 `{ success, error, meta }`。
- 时间字段统一 ISO8601 UTC；审计事件至少包含 `occurredAt` 与 `requestId`。

### Architecture Compliance

- 目录边界必须遵守：
  - 后端：`apps/api/src/modules/admin`（领域逻辑）+ `apps/api/src/infra`（基础设施）
  - 前端：`apps/web/src/features/admin`（业务）+ `apps/web/src/shared`（复用）
- 禁止把账号管理逻辑散落到 `auth` 或无关模块，避免后续权限链路耦合失控。
- 账户状态变更必须经统一服务层，不得在 controller 直接写状态机逻辑。

### Library / Framework Requirements

- NestJS v11 与 Node 22 兼容（官方迁移指南要求 Node >=20）；当前基线可继续使用。
- Redis 8 已出现多次安全修复（8.0.x），如用于导入任务队列需锁定稳定补丁版本并及时更新。
- 企业微信 OAuth2 集成按授权码模式落地，回调 URI 必须白名单精确匹配，避免开放重定向风险。

### File Structure Requirements

- 后端建议新增：
  - `apps/api/src/modules/admin/accounts/`*（controller/service/dto/entities）
  - `apps/api/src/modules/audit/`*（若已存在则复用，不重复造轮子）
- 前端建议新增：
  - `apps/web/src/features/admin/accounts/`*（page/components/api/store）
- 共享契约优先落在 `packages/contracts`，避免前后端重复定义 DTO。

### Testing Requirements

- 后端必须覆盖：
  - 创建/编辑/禁用/导入成功路径
  - 非法 roleId、重复账号、导入格式错误等失败路径
  - 审计记录存在性断言
- 前端必须覆盖：
  - 账号创建编辑提交流程
  - 禁用二次确认
  - 批量导入错误反馈与部分成功场景
- 保持 Story 1.1 已建立的质量门：`lint/test/build` 全绿后再进入 review。

### Previous Story Intelligence (Story 1.1)

- 已形成 monorepo 与目录基线，不要新建第二套工程结构；在既有结构内增量实现。
- 已全局注册 API 响应拦截与异常过滤，新增接口应直接复用统一 envelope。
- CORS 已收敛到白名单配置，新增管理 API 无需重新放开全域访问。
- 鉴权 token 持久化链路已修复，前端请求应沿用现有 auth store 机制，不要旁路读写 token。

### Git Intelligence Summary

- 最近提交表明基线工作已完成且修复项聚焦“统一规范落地”（响应格式、鉴权、测试）。
- 当前故事应延续“先契约后实现、先测试后合并”的节奏，避免回到脚手架式粗放开发。
- 变更集中在 `features/admin` 与 `modules/admin`，避免跨模块大范围改动引入回归。

### Latest Tech Information

- NestJS 11 官方迁移信息确认 Node 20+，当前 Node 22 满足要求；可继续作为运行时基线。
- Redis 8.0 系列在 2025-2026 有多项 CVE 修复，建议避免早期补丁并固定到最新稳定 patch。
- OAuth2 安全基线建议：`state` 防 CSRF、精确 `redirect_uri` 白名单、最小 scope、敏感 token 仅服务端保存。

### Project Context Reference

- 未发现 `project-context.md`；本故事上下文来自 `epics.md`、`architecture.md`、`需求全流程管理系统-PRD.md`、`ux-design-specification.md` 与 `1-1` 已完成故事。

### References

- [Source: `_bmad-output/planning-artifacts/epics.md`#Story 1.2: 管理员管理账号生命周期]
- [Source: `_bmad-output/planning-artifacts/需求全流程管理系统-PRD.md`#6.5 管理功能]
- [Source: `_bmad-output/planning-artifacts/architecture.md`#Core Architectural Decisions]
- [Source: `_bmad-output/planning-artifacts/architecture.md`#Project Structure & Boundaries]
- [Source: `_bmad-output/planning-artifacts/ux-design-specification.md`#Core User Experience]
- [Source: `_bmad-output/implementation-artifacts/1-1-从-starter-template-初始化项目基线.md`]

## Dev Agent Record

### Agent Model Used

gpt-5.3-codex-low

### Debug Log References

- NestJS v11 migration guide（Node 20+ 要求）
- Redis 8.0 release notes（8.0.x 安全修复）
- OAuth2 安全实践（授权码/PKCE、state、redirect_uri 白名单）
- `pnpm --filter @ai-demand/api test`
- `pnpm --filter @ai-demand/api test:e2e`
- `pnpm --filter @ai-demand/web test`
- `pnpm lint`
- `pnpm test`
- `pnpm build`

### Implementation Plan

- 按任务顺序落地：先后端账号生命周期 API 与审计，再前端账号管理核心流程，最后补齐单测与质量门。
- 后端采用 `AccountsService` 统一处理状态变更与角色校验，控制器仅做参数接入与路由编排。
- 审计服务统一记录成功与失败操作，失败至少包含 `reasonCode` 与 `requestId`。
- 前端在 `AccountManagementPanel` 聚合列表、编辑抽屉、禁用确认、批量导入入口，并统一“问题 + 原因 + 下一步”反馈文案。

### Completion Notes List

- Ultimate context engine analysis completed - comprehensive developer guide created
- Story 状态已设置为 `ready-for-dev`
- 已补齐实现任务、架构约束、前序故事情报、近期提交规律与最新技术风险提示
- 已完成后端账号生命周期接口：创建、编辑、禁用、批量导入，以及 roleId 存在性校验与统一 envelope 返回。
- 已完成审计记录：创建/编辑/禁用/导入成功与失败均记录 actor、target、before/after、occurredAt、requestId（失败含 reasonCode）。
- 已完成前端账号管理关键流程：列表、编辑抽屉、禁用确认弹层、批量导入入口与结果反馈。
- 已补充测试：后端 service 单测 + e2e；前端关键交互单测（提交、禁用确认、导入失败处理）。
- 已通过质量门：`pnpm lint`、`pnpm test`、`pnpm build` 全部通过。

### File List

- `_bmad-output/implementation-artifacts/1-2-管理员管理账号生命周期.md`
- `apps/api/src/common/interceptors/response.interceptor.ts`
- `apps/api/src/common/filters/http-exception.filter.ts`
- `apps/api/src/common/guards/admin-auth.guard.ts`
- `apps/api/src/app.module.ts`
- `apps/api/package.json`
- `apps/api/src/modules/audit/audit.module.ts`
- `apps/api/src/modules/audit/audit.service.ts`
- `apps/api/src/modules/admin/admin.module.ts`
- `apps/api/src/modules/admin/accounts/accounts.module.ts`
- `apps/api/src/modules/admin/accounts/accounts.controller.ts`
- `apps/api/src/modules/admin/accounts/accounts.service.ts`
- `apps/api/src/modules/admin/accounts/accounts.types.ts`
- `apps/api/src/modules/admin/accounts/accounts.service.spec.ts`
- `apps/api/test/app.e2e-spec.ts`
- `apps/web/src/api/client.ts`
- `apps/web/src/views/admin/AccountView.vue`
- `apps/web/package.json`
- `apps/web/src/features/admin/accounts/types.ts`
- `apps/web/src/features/admin/accounts/api.ts`
- `apps/web/src/features/admin/accounts/AccountManagementPanel.vue`
- `apps/web/src/features/admin/accounts/AccountManagementPanel.spec.ts`
- `packages/contracts/src/index.ts`
- `pnpm-lock.yaml`

## Senior Developer Review (AI)

**审查人：** Peng  
**审查日期：** 2026-04-09  
**结论：** Approved after fixes（已完成 HIGH/MEDIUM 自动修复并复测通过）

### Git 与 File List 对照

- 工作区存在未跟踪目录：`apps/api/src/modules/admin/accounts/`、`apps/api/src/modules/audit/`、`apps/web/src/features/admin/accounts/` 等；故事 File List 已列出路径，但 **尚未纳入版本库**，变更对团队不可见、不可回滚对比。
- 另有已修改未提交文件：`architecture.md`、若干 API/Web 文件与故事/sprint 元数据；与「仅应用代码」的边界混杂，需在提交时拆分或说明。

### 自动修复落地（选项 1）

- 已新增 `AdminAuthGuard` 并应用于 `AccountsController`，管理接口要求 `Authorization: Bearer <token>`，并增加未授权 e2e 断言。
- 已将账号与审计从纯内存改为文件持久化存储（`process.env.ACCOUNTS_DATA_FILE` / `process.env.AUDIT_DATA_FILE` 可覆盖），修复重启丢失问题。
- 已将审计 actor 改为从认证上下文提取，不再固定 `'admin-user'`。
- 已把账号相关 DTO/类型上收至 `packages/contracts`，前后端改为复用共享契约。
- 已细化导入失败原因码（`ROLE_NOT_FOUND` / `DUPLICATE_EMAIL` / `VALIDATION_FAILED` / `IMPORT_FAILED`）。
- 已新增 `import` 批次汇总审计事件，补齐导入操作的一等追溯语义。
- 已在 API envelope 的 `meta` 中补齐 `requestId`，并在前端 `ApiError` 透传用于用户反馈定位。
- `AuditService` 已支持按 `requestId` 查询（`list(requestId?)`）。

### 复测结果

- `pnpm lint` ✅
- `pnpm test` ✅
- `pnpm build` ✅

---

## Change Log

- 2026-04-09: 执行自动修复（选项 1）：完成鉴权与 actor 注入、持久化落地、共享契约收敛、导入审计与原因码增强、requestId 透传，并通过 lint/test/build；故事状态更新为 `done`。
- 2026-04-09: Senior Developer Review (AI)：结论 Changes Requested；故事状态 `review` → `in-progress`；登记 HIGH/MEDIUM/LOW 发现项。
- 2026-04-08: 完成 Story 1.2 开发实现，新增账号生命周期 API、审计记录、前端关键交互与测试覆盖，并通过 lint/test/build 质量门。